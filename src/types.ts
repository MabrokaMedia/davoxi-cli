// CLI-specific types (API data models are now imported from @davoxi/client)

export interface DavoxiConfig {
  api_key?: string;
  api_url?: string;
  access_token?: string;
  refresh_token?: string;
}
